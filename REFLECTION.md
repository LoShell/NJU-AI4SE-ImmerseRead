### 1. Superpowers 技能的实际作用
brainstorming发挥了较大作用，在这个过程中一个模糊的想法可以慢慢落地，有了大概的边界与框架，得到了合适的定义。但是writing-plans相对而言形式大于实质，其生成的计划走完只能得到一个很基本的产物，甚至很多时候会无法衔接，分配子agent的时候上下文并不算特别清晰，导致子agent的实现也很粗糙。在后续实现里，经过我和主agent的协调，实际的实现是远比plan里的内容要细致且丰富的。
### 2. TDD 是阻碍还是放大器
TDD 对确定性核心逻辑是放大器，对探索性 UI 和 LLM 效果调整有一定阻碍。在前期不涉及UI的骨架代码编写过程中，TDD可以帮助agent以标准且严谨的流程实现代码更新，防止了写新代码导致旧代码出问题的情况，是有效的工作流。但是在UI设计上，很多调整是细微的，UI 设计通常需要边实现、边观察、边调整。字体、颜色、间距和布局是否合适，很难完全通过传统单元测试表达。如果一次微小的颜色调整也机械地经历红—绿—重构，流程成本会高于修改本身。显然，应该把UI设计和前端开发放在靠后的task，并且更适合与主agent直接对话和完整工作流确认，而不是依赖TDD。
### 3. Subagent 的自主工作能力
经过观察，sub-agent的自主工作时间通常在20-40分钟，但是会受到权限和环境依赖阻断影响。在实际开发中遇到了一些问题，比如环境依赖。在plan里面node使用22版本，我安装了nvm用于管理不同版本的node，在任务开始之前使用的是16版本，无法满足plan里对于22版本的需要，所以sub-agent停摆了，但是也并不返回错误，而是锲而不舍地请求权限，在循环3次之后我察觉到不对，于是中断了子agent，去和主agent交互查探，主agent返回了环境依赖问题，我才明白原来是node版本问题。这个task用了足足40分钟。于是我和主agent协商，由它告诉我所有需要的环境依赖，而我自己下载依赖，麻烦才得以解决。
### 4.什么样的 task 颗粒度最好？
最优 task 应满足，只有一个明确目标；通常修改不超过 3–5 个相关文件；有一个可观察的验收结果；输入、输出和非目标明确；能在一次 subagent 会话中完成、失败时容易定位责任边界。
### 5.SPEC/PLAN 不清导致偏离的案例
中文TXT解析上传时遇到了乱码问题。我找到了一篇有256章的小说当作样本，上传解析之后只能得到乱码。在原本PLAN里面，TXT中文解析只考虑到UTF-8的情况，而那个TXT使用了GBK，导致解析问题，这是规约遗漏导致的问题。实际开发情况很复杂，所以修订后系统首先识别 UTF-8 BOM；无 BOM 时严格尝试 UTF-8，失败后尝试 GB18030；无法可靠解码时拒绝导入并提示用户转换编码。成功导入后统一保存为 UTF-8。
### 6.最有效的 prompt/context 策略
我认为是限定可操作文件边界并且提出疑问，先问后答再实现，和agent交互的过程中，首先分条分点列出自己的疑惑、思考和创意，然后让对方仅回答并给出方案，不编码。编码时严格限定文件边界，如果是不确定的修改范围，请在开始编码前commit一次。验证方式需要对方给出，如前端测试，需要给出vite的启动网址，进入并查看。一问一答的形式有助于agent和开发者都明确下一步的具体情况。
### 7.凭据与分发
key不能写入源码、git等，应该通过合适的方式帮助使用者配置，比如通过命令行或者脚本。分发则意味着环境同步和项目可分享，明确端口映射等，确保全新机器上可以从零启动。
### 8. 如果重新开发
如果重做我一定会把前后端分的更清晰。在开发初始，agent劝说我，这个项目后端并不庞大，建议我用next.js前后端协同开发。我虽然保留了 Spring Boot 后端，但部分业务仍落在前端工程中，最终形成了职责边界不清的问题。这种结构使后续 UI 重构受到较大限制。我与 Figma Make 交互得到的新设计无法直接迁移，而 Codex 根据截图模仿 UI 的效果也不理想。如果重做，我会先确定稳定的后端 API 和数据结构，使前端只负责展示、交互和状态管理。即使项目规模较小，清晰的职责划分仍然是持续开发和替换界面实现的前提。
### 9. 对 Superpowers 的批判
批判在认死理不懂变通，有的原本训练灵活的agent会在superpowers的加持下降智。Superpowers 能提高执行过程的纪律性，但不能保证产品判断正确。它把工程师的责任从“亲自写出每一行代码”转移到了定义边界、发现遗漏、设计验证方法和承担最终结果上。对本项目而言，它最有价值的地方不是让 AI 写得更快，而是让 AI 的行为更容易检查；它最大的局限则是容易把尚未稳定的探索性问题，过早包装成看似精确的 task。