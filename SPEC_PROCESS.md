# SPEC_PROCESS

日期：2026-08-06

## 1. 过程概述

本项目使用 Superpowers 工作流，从一个模糊想法“沉浸式小说阅读器”逐步收敛为 SPEC 与 PLAN。过程遵循：

1. `brainstorming`：澄清目标用户、产品边界、核心 LLM 体验和非 agent 约束。
2. `writing-plans`：把确认后的设计拆成可执行任务。
3. 人工审查：根据课程通用要求补充根目录交付文件、凭据安全、GitLab CI、冷启动验证要求。

当前已完成：

- 设计规格：`SPEC.md`
- 实现计划：`PLAN.md`
- Superpowers 原始设计记录：`docs/superpowers/specs/2026-08-06-immerseread-design.md`
- Superpowers 原始计划记录：`docs/superpowers/plans/2026-08-06-immerseread-implementation.md`

## 2. Brainstorming 关键节点

### 2.1 产品定位从“AI 阅读工具”收束为“个人沉浸阅读器”

初始想法包含 TXT 自动解读、BGM、流畅 UI、批注和 LLM 聊天。讨论后确认第一版主要面向小说和网文读者，而不是严肃文学读者或知识管理用户。

采纳决策：

- 第一版定位为“个人沉浸阅读器”。
- LLM 不是专家型文学分析器，而是“同好型书友”。
- 批注保持轻量，不做 Obsidian 式知识库。

### 2.2 防剧透成为核心工程亮点

原设想中 LLM 需要“知晓书籍、梳理脉络、帮助分析”。讨论后明确选择“只理解已读部分”，即读者读到哪里，LLM 最多只能看到哪里。

采纳决策：

- 书搭子只基于已读上下文回答。
- 防剧透不能只靠提示词，必须由 `SpoilerGuard` 从工程上裁剪上下文。
- 使用测试小说验证未读真相不会进入 LLM payload。

### 2.3 BGM 设计从“音乐搜索”转为“本地私有音频库 + 氛围匹配”

为了避免版权和平台边界问题，BGM 不做在线搜索、下载或分享。内置音频只使用免版权或自制演示素材，用户可以导入自己的本地音频。

采纳决策：

- 音频默认保存在浏览器本地。
- LLM 只输出当前片段氛围标签。
- BGM 推荐算法由确定性代码完成，便于测试。

### 2.4 技术栈从轻 Node 后端调整为 Spring Boot

最初建议后端使用 Node，是为了轻量代理 LLM。用户说明自己更熟悉 Java 后，技术栈调整为 React + Spring Boot + IndexedDB。

采纳决策：

- 后端使用 Spring Boot 4.1.0。
- 第一版仍不使用 MySQL，因为小说正文、批注、BGM 和聊天记录都应本地优先。
- 后端职责保持清晰：LLM 代理、凭据隔离、请求校验、日志脱敏。

## 3. 三轮关键迭代节选与处理

### 迭代 1：LLM 是否读取全书

问题：LLM “知晓书籍”的方式是什么？

选项：

- 只理解已读部分。
- 后台预解析全书，但聊天隐藏未读内容。
- 全书都给 LLM，用提示词要求不剧透。

处理：用户和 Codex 都选择第一种。原因是它最符合“陪你一起读”的产品感，也最容易用测试证明不剧透。

### 迭代 2：陪伴感怎么做

问题：过去做过提示词，但人机感重，如何让书搭子更自然？

处理：不把陪伴感押在复杂人设提示词上，而是通过四层实现：

- 共同上下文：能引用刚读过的具体内容。
- 克制回复：默认 1-4 句。
- 同好语气：适度吐槽、一起猜，不像专家。
- 模式边界：回忆、吐槽、一起猜、解释当前段落、防剧透问答。

### 迭代 3：后端和数据库选择

问题：为什么不用 Spring Boot + MySQL？

处理：最终选择 Spring Boot，但不引入 MySQL。Spring Boot 符合用户熟悉的技术栈；不使用 MySQL 是为了保持“小说内容不上传服务器”的隐私和版权边界。

## 4. AI 建议的采纳与修正

采纳的建议：

- 将产品第一版定位为“个人沉浸阅读器 + 不剧透书搭子”。
- 将防剧透做成独立工程模块。
- 将 BGM 推荐拆成 LLM 氛围分析和确定性匹配算法。
- 默认回复短，而不是专家型长分析。

被用户修正的建议：

- 后端技术栈从 Node 调整为 Spring Boot，因为用户更熟悉 Java。
- 批注功能保持轻量，不做复杂人物索引或知识库。
- 阅读交互采用用户主动触发 + 章节结束轻提示，不做阅读中自动弹幕式陪读。

## 5. 计划生成后的人工补充

阅读课程通用要求后，发现原计划还需要补充：

- 根目录 `SPEC.md` 和 `PLAN.md`。
- `SPEC_PROCESS.md` 和 `AGENT_LOG.md`。
- GitLab CI 配置，且必须包含名为 `unit-test` 的 job。
- 凭据安全从单纯 `.env` 增强为系统凭据管理器优先、`.env` fallback。
- 冷启动验证要求。
- 最终 WebUI 部署 URL 要求。

这些补充已写入 `SPEC.md` 和 `PLAN.md`。

## 6. 冷启动验证记录

状态：待冷启动验证后补充。

计划操作：

1. 新开一个不同 agent 类型或不同入口的全新对话。
2. 不导入本对话历史，不提供额外口头解释。
3. 只提供 `SPEC.md`、`PLAN.md` 和 `docs/COLD_START_PROMPT.md` 中的任务说明。
4. 要求第二个 agent 尝试实现 PLAN 中 1-2 个 task，遇到不确定之处立即暂停询问。
5. 将它暴露的问题、误解、暂停点和产出差距记录到本节。
6. 根据冷启动反馈修订 SPEC / PLAN，并记录关键 diff。

## 7. 初步反思

`brainstorming` 技能在本项目中最有价值的地方，是逼迫产品从“很多功能”收束到“一个核心体验”：不剧透的沉浸式网文书搭子。它也暴露了一个问题：课程要求较多，单靠产品讨论容易遗漏 CI 命名、冷启动验证、凭据管理细节等交付项，因此需要在写完 SPEC/PLAN 后再用课程要求反向审查。

